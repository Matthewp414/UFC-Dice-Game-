function roll(sides){
	Math.ceil((Math.random( * sides)  );
}
function fightBout(health,ap,ep,bp,weight){
	// How do I store my users variables in a function and pass them into the fight and change them if they arent global
}

